<fieldset>
    <legend>
        <div class="title">
            <h1>{{game.title}}</h1>
            <p class="releaseDate">
                <i>{{dateFormat game.releaseDate}}</i>
            </p>
        </div>
    </legend>

    <img src="/{{game.filename}}" alt="Icon - {{game.title}}" class="gameImage" />
    <ul>
        <li><i>editeur :</i> <b class="boldList">{{game.editor.name}}</b></li>
        <li><i>genre :</i> <b class="boldList">{{game.genre.name}}</b></li>
    </ul>
    <p class="desc">{{game.desc}}</p>
    {{> editButtons id=game.id highlighted=game.highlighted}}
</fieldset>
