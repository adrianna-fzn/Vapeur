<h1>{{game.title}}</h1>
<img src="/{{game.filename}}" alt="Icon - {{game.title}}" />
<ul>
    <li>{{game.title}}</li>
    <li>{{game.desc}}</li>
    <li>{{dateFormat game.releaseDate}}</li>
    <li>{{game.editor.name}}</li>
    <li>{{game.genre.name}}</li>
</ul>